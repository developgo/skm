workspace:
  base: /go
  path: src/github.com/developgo/skm
  
pipeline:
  test:
    image: golang:latest
    commands:
      - go vet
      - go test -v -cover
  build:
    image: golang:latest
    commands:
      - go build -ldflags "-X main.Version=${DRONE_TAG}" -o dea
      - go test -v
